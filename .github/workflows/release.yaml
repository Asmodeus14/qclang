name: Release Static Binaries

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: write  # Required for creating releases

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      run: |
        rustup target add x86_64-unknown-linux-gnu
        rustup update stable
    
    - name: Build Linux Static
      run: |
        RUSTFLAGS='-C target-feature=+crt-static' cargo build --release --target x86_64-unknown-linux-gnu
    
    - name: Upload Linux Binary
      uses: actions/upload-artifact@v4  # ✅ UPDATED TO v4
      with:
        name: qclang-linux
        path: target/x86_64-unknown-linux-gnu/release/qclang

  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      run: |
        rustup target add x86_64-pc-windows-msvc
        rustup update stable
    
    - name: Build Windows
      run: cargo build --release --target x86_64-pc-windows-msvc
    
    - name: Upload Windows Binary
      uses: actions/upload-artifact@v4  # ✅ UPDATED TO v4
      with:
        name: qclang-windows
        path: target/x86_64-pc-windows-msvc/release/qclang.exe

  release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    
    steps:
    - name: Download Linux Artifact
      uses: actions/download-artifact@v4  # ✅ UPDATED TO v4
      with:
        name: qclang-linux
        
    - name: Download Windows Artifact
      uses: actions/download-artifact@v4  # ✅ UPDATED TO v4
      with:
        name: qclang-windows
        
    - name: Create Release Packages
      run: |
        # Create release directory
        mkdir -p release
        
        # Package Linux binary
        mv qclang release/qclang-linux-x86_64
        tar -czf release/qclang-linux-x86_64.tar.gz -C release qclang-linux-x86_64
        
        # Package Windows binary  
        mv qclang.exe release/qclang-windows-x86_64.exe
        zip release/qclang-windows-x86_64.zip release/qclang-windows-x86_64.exe
        
        # Create checksums
        cd release
        sha256sum * > sha256sums.txt
        ls -lh
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: release/*
        generate_release_notes: true